CREATE TABLE "SYSTEM"."BOOK_STORE" 
   (	"BKID" NUMBER(3,0) NOT NULL ENABLE, 
	"BOOKNAME" VARCHAR2(20 BYTE), 
	"AUTHOR" VARCHAR2(20 BYTE), 
	"STATUS" VARCHAR2(10 BYTE), 
	"CATEGORY" VARCHAR2(10 BYTE), 
	 CONSTRAINT "TABLE100_PK" PRIMARY KEY ("BKID")